# 人工 courtyard 挪位清单（连铜一起挪）

> **活动目标最新桥路检查点**：ERC **0/0**；普通DRC错误 **0**，仍有 **22未连接、119警告**，一致性0，**NOT_FAB_RELEASED**。主差分输入已成对、同面、零过孔；J3物理顺序已旋转。TIM2寄存器后端模型测试通过，23项host＋2项MCU测试通过，最终ELF/板上未完成。详见 [桥路与时基检查点](ANALOG_CHECKPOINT_20260923_CN.md)。下方旧数字为历史。


日期：2026-09-09。官方 DRC 现有 **10** 处封装重叠。脚本在已布线板上改坐标会把焊盘压到旧铜（CP05：错误 108→175），**不要再跑** `tools/nudge_courtyard_revA2.py`。

在 KiCad PCB 编辑器里：选中封装后用 **D**（拖动，走线跟着焊盘），不要只用改坐标。

当前冲突（`drc_revA2_kicad10.json`）：

| 冲突 | 建议 |
|---|---|
| U1 与 C3 | C3 是 IMU 去耦，沿 U1 上方空隙拖到约 y=8.0，离开 TSSOP courtyard |
| U6 与 R3 | R3 是 MCP73831 编程电阻，拖到 U7 附近，不要靠 LDO |
| U6 与 C11 / C12 | C11/C12 跟着 U6 的 IN/OUT，整组往板底中间挪，离开 LED |
| U6 与 D1 | D1/R6 留在板底角落，U6 不要压 LED |
| C11 与 D1 / C12 | 拉开 0402 间距 ≥ 0.5 mm courtyard |
| R7 与 Q1 | 都在背面，拖开 LED 栅极电阻与 MOSFET |
| C7 与 C10 | 右侧 0402 柱，把 C10 沿 x=10.55 再往下移约 1.2 mm |

挪完后只跑官方 DRC，不要重新自动布线。目标先把 `courtyards_overlap` 降到 0；短路和未连接仍按单网手工处理。
